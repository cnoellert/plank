# Script entry points

- `build/`: pinned dependency preparation and product compilation.
- `package/`: package assembly, versioning, runtime checks and artifact collection.
- `test/`: qualification probes, regression checks and report runners.
- `maintenance/`: explicit development install/uninstall and maintenance tools.

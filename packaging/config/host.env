# Shared host-worker settings loaded by the boot-time supervisor.
# Keep credentials out of this file; values become worker environment data.
STATIONCONNECT_HOST_OPTIONS=address_family=both cert=/etc/stationconnect/tls/cert.pem pkey=/etc/stationconnect/tls/key.pem

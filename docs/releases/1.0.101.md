# PLANK 1.0.101

Rebuild from the final Host/Client source repositories and pinned dependency
histories. This is not a streaming-behavior change relative to the 1.0.100
preparation packages.

- Preserve upstream source, licenses and useful development history.
- Use stable project repository URLs and remapped, verified dependency pins.
- Keep optional administrator-enabled wake requests disabled by default.
- Keep infrastructure products independent of the Host/Client source and builds.

Package validation and hardware/session acceptance are recorded separately in
HANDOFF.md and the artifact catalog. A mainline package is not automatically
a published or hardware-qualified release.

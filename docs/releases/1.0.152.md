# PLANK 1.0.152 candidate

## Client

- Follow the client's primary display when ordering a virtual Linux desktop.
- Preserve the existing presentation canvas and image proportions when Host
  virtual modes differ from Client monitor sizes.
- Keep manual two-display bookmarks usable with other local monitor counts or
  arrangements by omitting an ambiguous optional primary-display hint.

## Host

- Linux: bind the first virtual connector to the selected primary side during
  both live layout changes and login-screen preparation.

This candidate integrates Client #6, Linux Host #9 and root #10. It does not
include Client #7's separate Wacom-focus proposal. Host and supervisor binaries
must come from the same package. CI is not live display/input acceptance;
macOS 27 multi-monitor qualification remains outstanding. No release publication
or remote installation is implied.

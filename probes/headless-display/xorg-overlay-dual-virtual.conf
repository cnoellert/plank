# Qualification-only overlay loaded after Autodesk's canonical xorg.conf.
# The default layout selects the StationConnect screen without copying or
# modifying the canonical Autodesk configuration and its input classes.

Section "ServerFlags"
    Option "DefaultServerLayout" "StationConnect Headless Probe Layout"
EndSection

Section "ServerLayout"
    Identifier "StationConnect Headless Probe Layout"
    Screen 0 "StationConnect Headless Probe Screen" 0 0
EndSection

Section "Device"
    Identifier "StationConnect NVIDIA GPU"
    Driver "nvidia"
    Option "AllowEmptyInitialConfiguration" "True"
    Option "ConnectedMonitor" "DFP-0, DFP-2"
    Option "UseDisplayDevice" "DFP-0, DFP-2"
    Option "CustomEDID" "DFP-0:/run/stationconnect/headless-probe/virtual-1.edid; DFP-2:/run/stationconnect/headless-probe/virtual-2.edid"
    Option "MetaModes" "DFP-0: 1920x1080 +0+0, DFP-2: 1920x1080 +1920+0"
    Option "nvidiaXineramaInfoOrder" "DFP-0, DFP-2"
    Option "HardDPMS" "False"
    Option "Interactive" "False"
EndSection

Section "Screen"
    Identifier "StationConnect Headless Probe Screen"
    Device "StationConnect NVIDIA GPU"
    DefaultDepth 30
    SubSection "Display"
        Depth 30
        Virtual 3840 1080
    EndSubSection
EndSection

[package]
name = "connect-probe-datasmash"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
publish = false

[dependencies]
anyhow = "1.0"
bytes = "1.10"
hex = "0.4"
kynet = { path = "../../../third_party/kyber-kymux/kynet", features = ["kynet-quinn"] }
subtle = "2.6"
tokio = { version = "1.47", features = ["io-util", "macros", "net", "rt-multi-thread", "sync", "time"] }

[workspace]
